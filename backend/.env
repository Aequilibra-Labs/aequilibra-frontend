HL_ENV=mainnet               # or mainnet
# Optional explicit base (auto from HL_ENV if omitted)
# HL_API_URL=https://api.hyperliquid-testnet.xyz
# HL_API_URL=https://api.hyperliquid.xyz

# Optional: encrypt & persist agents (recommended for prod)
# ENC_KEY=<32-byte urlsafe base64 (or 64-hex)> 
DB_PATH=./agents.db
